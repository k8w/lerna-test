{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^5.1.8"
  },
  "scripts": {
    "version:dev": "lerna version --conventional-commits --conventional-prerelease --preid dev",
    "version:prod": "lerna version --conventional-commits --conventional-graduate",
    "pub": "lerna publish from-package --pre-dist-tag dev"
  }
}